{
  "name": "zk-livepoker-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.7",
    "ethers": "^6.12.1",
    "hardhat": "^2.22.1"
  }
}
